<Project Sdk="Microsoft.NET.Sdk">
  <!--
    Identity only. Shared TFM / refs / RimWorldDir / Harmony live in
    ColonySeries/Directory.Build.props + Directory.Build.targets.
  -->
  <PropertyGroup>
    <AssemblyName>DeadDrop</AssemblyName>
    <RootNamespace>DeadDrop</RootNamespace>
    <ColonySeriesNeedsHarmony>true</ColonySeriesNeedsHarmony>
  </PropertyGroup>
</Project>

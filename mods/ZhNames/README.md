# 中文姓名 ZhNames

为普通人类殖民者生成高质量随机中文姓名（姓氏含常见复姓 + 按性别语素组合的二字/单字名，防同字、防重名）。

- 只接管走默认名字库（`PawnNameCategory.HumanStandard`）的命名；
- 部落、帝国、异种（xenotype）、特殊背景、Creepjoiner 等原有规则包命名完全不受影响；
- 姓名结构用 `NameTriple(名, 名, 姓)`，亲缘姓氏识别正常；
- 设置：总开关、二字名概率（默认 80%）。

依赖：Harmony。
